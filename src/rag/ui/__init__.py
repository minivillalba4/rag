"""Capa de presentación: Gradio.

No importa nada de `langchain_*` ni de `infrastructure` directamente; el
único acoplamiento permitido es con `application` (a través del bundle
RAG expuesto por `bootstrap.AppServices`).
"""
